---
- name: Prepare
  hosts: all
  vars:
    platform:
      Darwin:
        become: false
  tasks:
    - name: Full system upgrade
      become: true
      community.general.pacman:
        update_cache: true
        upgrade: true
      when:
        - ansible_os_family == "Archlinux"
